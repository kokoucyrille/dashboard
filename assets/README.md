# Assets

Dépose ici un éventuel logo ou drapeau en image (ex. `flag_togo.svg`) si tu
veux remplacer l'emoji 🇹🇬 utilisé dans le header par un visuel — il suffira
de l'appeler dans `components/header.py` avec `st.image()` ou en l'encodant
en base64 dans le HTML du header.
